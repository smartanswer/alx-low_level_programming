#include <stdio.h>
/**
 * main - The program that print single digit
 *
 * Return: always 0 (success)
 */
int main(void)
{
int a;
for (a = 0; a < 10; a++)
printf("%d", a);
printf("\n");
return (0);
}
