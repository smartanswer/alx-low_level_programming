#include <stdio.h>
/**
 * main - The program that print single digit
 *
 * Return: always 0 (success)
 */
int main(void)
{
int a;
for (a = 48; a < 58; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
